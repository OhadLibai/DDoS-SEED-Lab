FLOW:

BEFORE PRESENTATION - 
Bonus slowloris:
./setup-gcp-infrastructure.sh 

Flow control:
./setup-gcp 
./deploy-server.sh gcp adaptive-slow


HTTP2 flood:

./gcp-setup-infrastructure.sh vlm-fga
# Share external IP with Ohad
gcloud compute ssh http2-flood-lab --zone=us-central1-a
git clone https://github.com/OhadLibai/DDoS-SEED-Lab.git
cd DDoS-SEED-Lab/http2-app-flood/
./deploy-server.sh --gcp part-B SCENARIO=crypto WORKLOAD=5
docker exec prod-victim-server sh -c "awk 'NR>1 && \$4==\"01\" {count++} END {print count+0}' /proc/net/tcp" &&
curl --http2-prior-knowledge -w "Time:%{time_total}s\n" -o /dev/null -s http://$EXTERNAL_IP:8080